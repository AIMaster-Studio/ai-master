# AI Master 环境变量配置模板
# 复制此文件为 .env 并填入实际值，.env 已被 .gitignore 忽略，不会提交到仓库。
# 生产环境（如 Vercel）请在平台面板中配置环境变量，不要使用 .env 文件。

# DeepSeek API Key（必填，用于 AI 讲解复评）
# 获取地址：https://platform.deepseek.com/
DEEPSEEK_API_KEY=

# 服务端口（可选，默认 8787）
PORT=

# Turso 远程数据库（用于公网部署的数据持久化）
# 不设置则使用本地 SQLite（桌面端/本地开发/单元测试）
# 可通过 Vercel Storage 集成 Turso；配额以当前套餐为准。
# Vercel 集成默认注入 TURSO_DATABASE_URL；旧名称 TURSO_URL 仍兼容。
# 两者同时设置时必须指向同一数据库，否则拒绝启动。
TURSO_DATABASE_URL=
TURSO_URL=
TURSO_AUTH_TOKEN=

# ---------------------------------------------------------------------------
# RAG 嵌入模型（可选）
# ---------------------------------------------------------------------------
# 不配置时使用「本机哈希嵌入」：零依赖、不联网，但**只反映词面重合、不是语义检索**，
# 同义改写与跨语言提问会漏召回。检索结果里会如实标注这一点。
# 配置后走 OpenAI 兼容的 /embeddings 接口，得到真正的语义向量。
# ⚠️ 换嵌入模型后必须重建索引：不同嵌入模型的向量空间不可混用，服务端会直接拒绝。
EMBEDDING_BASE_URL=
EMBEDDING_MODEL=
EMBEDDING_API_KEY=
EMBEDDING_TIMEOUT_MS=

# ---------------------------------------------------------------------------
# Agent 能力（可选）
# ---------------------------------------------------------------------------
# 「深度研究」这类 agent 循环能力的单次模型调用超时（毫秒，默认 60000）
AGENT_TIMEOUT_MS=

# AI 复评超时（毫秒，默认 60000）
AI_REVIEW_TIMEOUT_MS=

# ---------------------------------------------------------------------------
# 公网暴露时的管理接口令牌（重要）
# ---------------------------------------------------------------------------
# 服务默认只绑定 127.0.0.1。一旦通过 PORT 或 AIMASTER_ALLOWED_HOSTS 对外暴露，
# 回环对端不再有鉴别力，以下写操作将**一律要求**本令牌，否则 403：
#   写模型配置、建/删知识库、入库文档、重建索引、清空记忆、安装/卸载技能。
# 未配置该变量时，暴露模式下这些接口全部不可写（fail-closed）。
AIMASTER_CONFIG_TOKEN=

# 远程内容管理令牌（仅用于无服务器/云端部署维护知识库与记忆）。
# 云函数的 TCP 对端永远不是回环，因此本机门禁在云上无法通过；配置本变量后，
# 携带请求头 X-AIMaster-Admin-Token 的调用方可执行建/删知识库、入库、重建索引、课程入库。
# 不配置即完全关闭（fail-closed）；该令牌不能用于写模型密钥，/api/ai/config 仍只允许本机。
# 请使用高强度随机值，并按平台密钥方式保存，不要写入代码库。
AIMASTER_REMOTE_ADMIN_TOKEN=

# 显式 Host 允许名单（逗号分隔，如 xxx.trycloudflare.com）
AIMASTER_ALLOWED_HOSTS=
