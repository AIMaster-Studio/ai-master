#!/bin/sh
# AIMaster 自动开发日志：每次提交后自动生成/追加日志
repo=$(git rev-parse --show-toplevel) || exit 0
cd "$repo" || exit 0
if command -v python >/dev/null 2>&1; then
  python scripts/auto_dev_log.py || true
elif command -v python3 >/dev/null 2>&1; then
  python3 scripts/auto_dev_log.py || true
fi
exit 0
