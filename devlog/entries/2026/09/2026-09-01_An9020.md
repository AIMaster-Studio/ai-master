
## 2026-09-01T18:46:47+08:00 - fix: 减少动效模式下内容被隐藏（beautify 渐入动画）

- **提交**：`99f1774c`
- **作者**：An9020 <1782694139@qq.com>
- **角色/地区**：- / -
- **分类**：问题排查
- **涉及文件**：
  - frontend/static/beautify/beautify.js
- **说明**：

### 背景

桌面版所有页面出现"只剩背景图、内容不可见"：系统开启"减少动效"（`prefers-reduced-motion: reduce`）后，`beautify.js` 的 `initObserver()` 直接 `return`，IntersectionObserver 从不运行 → 所有被标为 `.am-reveal` 的内容块永远停在 `opacity: 0`（beautify.css 中 `.am-reveal{opacity:0}`，仅 `.am-visible` 才显示）。

### 修复

- `applyReveal()` 开头：减少动效模式直接 `return`，不再给内容块添加隐藏类；
- `initObserver()` 减少动效分支：立即把所有已标记元素设为可见（防御性兜底）。

正常用户（未开减少动效）的滚动渐入动画不受影响。43 个引用 beautify.js 的页面一处修复全部生效。

### 验证

Electron 实测：首页 `sceneOpacity:1`、仪表盘 `heroOpacity:1`，内容全部可见，导航栏正常。

> 自动生成于 2026-09-01T19:18:28

## 2026-09-01T19:44:46+08:00 - fix: 全面修复导航链接白屏（目录链接补全 index.html + JS 动态链接修正）

- **提交**：`a6da497`
- **作者**：An9020 <1782694139@qq.com>
- **角色/地区**：- / -
- **分类**：问题排查
- **涉及文件**：
  - electron/main.js
  - agentic-engineering-cg/app.js
  - agentic-engineering-cg/index.html
  - frontend/data/knowledge-universe.json
  - frontend/static/agentic_cg/app.js
  - frontend/static/agentic_cg/index.html
  - frontend/static/ai_odyssey.html
  - frontend/static/bpe_game.html
  - frontend/static/claude_cg/index.html
  - frontend/static/interview.html
  - frontend/static/jj_interview.html
  - frontend/static/js/knowledge_stars.js
  - frontend/static/js/main.js
  - frontend/static/js/transition.js
  - frontend/static/llm_intro.html
  - frontend/static/llm_training_game.html
  - frontend/static/prompt_cg.html
  - frontend/static/prompt_cg_starlab/index.html
  - frontend/static/rag_cg/index.html
  - frontend/static/transformer_cg.html
- **说明**：

### 背景

桌面版（Electron + file://）多处导航白屏：44 个 HTML 页面全量渲染扫描 + 全部链接静态审计发现，白屏源于链接写法问题（页面本身都是成品，非半成品）：

1. **目录形式链接缺 `index.html`**：如仪表盘 `../canvas/`、CG 页"开始学习"按钮 `../../chapter/4`（含无尾部斜杠形式）——file:// 下目录 URL 不会自动补 index.html，直接 `ERR_FILE_NOT_FOUND` 白屏，且导航栏注入时机（`did-finish-load`）不触发导致无法切换页面；
2. **JS 运行时覆盖 href 为旧链接**（`agentic_cg`、`llm_intro`、`knowledge_stars` 等 7 处）；
3. **指向不存在的文件**：`agentic-engineering.html`（从未存在于仓库）；
4. **星图数据 57 个目录式 URL**（`../chapter/N/#kp-N`）；
5. **假链接占位符**：两个游戏"返回"按钮指向 `example.com`。

### 修复

- `electron/main.js`：新增 `will-navigate` 拦截 —— 检测到 file:// 目标是目录（含带/不带尾部斜杠、带锚点 hash）时自动补全 `index.html`（运行时兜底，覆盖所有页面）；
- 10 个 HTML：CG 页"开始学习"、返回/品牌链接补全 `index.html`；
- 7 个 JS：动态 href 默认值修正；
- `knowledge-universe.json`：57 个星图 URL 补全（保留 `#kp-N` 锚点）；
- `bpe_game.html` / `llm_training_game.html`：假链接改为返回 LLM 导论页。

### 验证

- 静态审计：0 损坏链接；
- 运行时扫描：44/44 页面渲染正常，无白屏；
- 点击链路测试 13/13 通过（CG→章节、返回仪表盘、星图按钮、转场跳转）；
- 知识星图"仪表盘"按钮、第 9/10 章等此前白屏入口全部恢复正常。

> 补充记录于 2026-09-02
