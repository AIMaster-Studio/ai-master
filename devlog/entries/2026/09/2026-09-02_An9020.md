
## 2026-09-02T18:17:08+08:00 - fix: 修复章节页背景占位与学习中心跳转白屏

- **提交**：`537c602c`
- **作者**：An9020 <1782694139@qq.com>
- **角色/地区**：- / -
- **分类**：问题排查
- **涉及文件**：
  - frontend/assets/chapter-demo.css
  - frontend/static/bpe_game.html
  - frontend/static/js/learning_coach_static.js
  - frontend/static/llm_training_game.html
- **说明**：

### 背景

桌面版（Electron + file:// 本地页面）排查中连续发现两类白屏/布局问题，本次一并修复：

1. **章节页首屏只剩背景**：`#demo-stars.dash-space` 背景星图 canvas 的正确样式（`position:fixed`）定义在 `dashboard-demo.css`，但章节页（第 1~10 章）只加载 `chapter-demo.css`，缺该规则 → canvas 以 static 布局占满一整个视口高度，把章节标题与内容推到首屏之外，需下滑才能看到。
2. **学习中心（学习教练）跳转白屏**：`interview.html` 位于 `frontend/static/`，其学习计划由 `learning_coach_static.js` 动态生成 URL —— 同目录页面误加 `../`（如 `../llm_intro.html` 被解析到不存在的 `frontend/llm_intro.html`），章节链接多一级（`../../chapter/9/` 解析到不存在的 `ai-master/chapter/9/`），点击"进入学习"即白屏。另发现 `bpe_game.html`、`llm_training_game.html` 的"返回"按钮存在同类层级错误。

### 修复内容

- `chapter-demo.css`：补充 `.dash-space{position:fixed;z-index:-1;inset:0;overflow:hidden;pointer-events:none}`，与 `dashboard-demo.css` 保持一致，一处覆盖全部 10 个章节页。
- `learning_coach_static.js`：
  - 计划 URL 层级修正：同目录目标去掉 `../`（`llm_intro.html`、`transformer_cg.html`、`rag_cg/index.html`、`prompt_cg_starlab/index.html`、`agentic_cg/index.html`）；章节目标 `../../chapter/N/` 改为 `../chapter/N/`。
  - localStorage 键升级为 `aimaster_frontend_plan_v2`：旧版本生成的计划中保存了错误 URL，若沿用旧键，修复后恢复旧计划仍会白屏；升级键后旧计划作废，重新生成即可。
- `bpe_game.html` / `llm_training_game.html`：返回按钮 `../llm_intro.html` 修正为同目录 `llm_intro.html`。

### 验证

在 Electron 桌面版中逐一实测（真实点击导航）：

- 章节页：第 1/2/7/10 章内容区回到首屏顶部（y≈152），标题直接可见；
- 学习教练默认路线 → `llm_intro.html`（内容 3310 字）；
- RAG 路线第 4 关 → `frontend/chapter/9/index.html`；Agent 路线第 4 关 → `frontend/chapter/10/index.html`；
- BPE / LLM 训练游戏"返回" → `llm_intro.html`。

> 自动生成于 2026-09-02T18:17:17
