# Runtime snapshot switch and secure push recovery — 2026-09-18

Resolved Git transport failure without disabling TLS verification: exported public root certificates already trusted in Windows LocalMachine/CurrentUser Root stores into the parent tools/windows-trusted-roots.pem; used per-command OpenSSL CA verification and the existing gh auth git-credential helper. No private keys or tokens exported, no global Git config changed. The first normal credential-helper push stalled and was interrupted; explicit gh helper push succeeded. Commit d7ad568 is now on origin/fix/vercel-production-audit. Initial Actions results: 35309669571 success, 35309669593 in progress; workflow identity/final outcome must be checked before claiming full CI success.

Added AIMASTER_DURABLE_SNAPSHOTS=1 opt-in to Vercel runtime. Requires complete remote credentials. createApp builds a shared SQL snapshot repository from remote learning-store client and ready promise, supplies RAG and per-user memory adapters; explicit injected repositories remain supported. Local SQLite cannot silently masquerade as remote snapshot mode. Status now identifies RAG/memory as configured remote snapshots while skills/agent remain ephemeral and filesPersistent remains false. This reports configuration, not proven remote health.

Two runtime guard/status tests added. npm run verify passed 170 tests plus frontend structural verification. Remote database round-trip and actual Vercel cold start still NOT tested. No Vercel environment flag set and no deployment triggered. Existing Preview-only free Turso database remains available. Future preview acceptance must confirm runtime storage health, write conflicts, capacity and the linked code source before production promotion.
