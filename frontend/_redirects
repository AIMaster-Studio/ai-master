# 前端发布根目录的重定向规则（publish root = frontend/）。
#
# 读取方（发布根已统一为 frontend/，本文件因此是**跨平台**的生效规则文件）：
#   - Cloudflare Pages：从构建输出目录（frontend/）读取 _redirects。
#   - Netlify：netlify.toml 的 publish 已设为 frontend/，本文件随即成为其生效规则；
#     且 Netlify 先处理 _redirects、后处理 netlify.toml 的规则（官方 rule processing order）。
#
# 单页应用路由：所有未匹配的路径回退到 index.html
# /*    /index.html   200

# 根路径重定向到学习中心
/    /learning-center/   302
