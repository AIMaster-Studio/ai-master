# AI Master 项目说明

更新日期：2026-09-09。面向大学生 AI 通识与 RAG 入门学习，当前为**纯静态前端 + 可选云服务**的原型：课程与学习工作台打包到 Cloudflare Pages，无需后端服务器；Firebase Realtime Database 与 DeepSeek API 为可选增强，未配置时自动降级。

## 核心流程

学习者做基础诊断，选择目标与节奏，进入核心任务，阅读材料并用自己的话解释。系统给出有明确来源的讲解反馈，再使用确定标答评价客观题。讲解条件满足且测验至少 75% 才记录通关；未通过可以重读、修订和重试，结果进入浏览器 localStorage（默认）或 Firebase Realtime Database（可选）与复习安排。

讲解检查属于练习反馈，存在规则漏判、模型误判与外部代答风险。通关代表满足当前任务规则，不是长期掌握、职业能力或防作弊考试认证。尚无可报告的教育效果提升、真实用户留存或营收数据。

## 架构与部署

| 层 | 当前实现 | 边界 |
| --- | --- | --- |
| 页面 | 原生 HTML、CSS、JavaScript | 沿用现有课程与交互页面 |
| 3D | 本地 Three.js r128 | 浏览器支持与实际性能需分别测试 |
| 内容 | `frontend/data/` 中的章节、课程图与核心题库 JSON | 原课程待审校内容不自动当作正确题库 |
| 托管 | Cloudflare Pages 静态部署 | 单文件 ≤ 25 MiB；不提供服务器端逻辑 |
| 学习工作台 | `learning-workspace.js` + `learning-local-fallback.js` | 后端 API 不可用时降级到 localStorage |
| 记录 | 浏览器 localStorage（默认）+ Firebase Realtime Database（可选） | Firebase 未配置时数据不跨设备同步 |
| 讲解评价 | DeepSeek API 直调 + 本地规则降级 | 密钥写入 `ai-config.js`，公网可见 |
| 客观判题 | 前端预设标答 | 不由模型决定客观题答案 |
| 桌面入口 | 已有 Electron | 与浏览器服务分别验收，分发携带适用资源与许可 |

**生产部署**：https://ai-master-aw5.pages.dev/
**学习工作台**：https://ai-master-aw5.pages.dev/learning-center/
**部署方式**：`npx wrangler pages deploy ./frontend --project-name=ai-master --branch=main --commit-dirty=true`

启动与检查命令见 [README](README.md)。静态托管可访问原课程与学习工作台，但 Firebase 未配置时不提供跨设备同步、DeepSeek 未配置时不提供 AI 复评，两者均明确降级。远程模型会接收必要的讲解及评价上下文，不能宣称所有数据永不出机。

## 配置文件

| 文件 | 默认状态 | 启用方法 |
| --- | --- | --- |
| `frontend/static/js/firebase-config.js` | 占位符（自动降级 localStorage） | 替换为 Firebase Console 中的 Web 应用配置 |
| `frontend/static/js/ai-config.js` | `apiKey` 为空（自动降级本地规则） | 填入 DeepSeek API Key |
| `frontend/_redirects` | 已配置根路径重定向到学习中心 | 无需修改 |
| `frontend/_headers` | 已配置安全头与静态缓存 | 无需修改 |

> ⚠️ **密钥可见性**：纯静态部署无法用环境变量隐藏 API Key。`ai-config.js` 中的 DeepSeek 密钥会被任何访问者从浏览器开发者工具读取。仅使用低配额密钥；如需隐藏密钥，应改为 Cloudflare Worker 代理方案（未在本原型实现）。

## 内容组织

原知识宇宙有十章、五十七个节点，涵盖大模型基础、Transformer、提示词、框架与 Agent、Claude Code、RAG、云平台与认证、部署与安全、项目实战及产品介绍。节点数是结构规模，不意味着每个节点已有完整正文、教师审校或能力验收。

本次七个核心模块包括大模型生成基础、可验收提示、Transformer、RAG 检索、RAG 评估、Agent 工具调用和安全边界，具体见 [核心课程与题库](frontend/data/learning-curriculum.json)。

[认知教学设计](frontend/data/knowledge-cognitive-map.json) 对 57 节点逐项标注目标、活动、证据和建议先修。层级依任务行为确定：BPE 演算为应用，框架选型为评价，项目交付为创造，产品介绍只标理解。原图展示连线不自动视为已验证先修。第七章首节点正文缺失标为 `missing`，不列为已完整覆盖。

## 交互与资产

保留已有课程首页、知识星海、思维画布、实验和高级交互入口。鲸鱼娘原形象和全部已有动作保留，用于陪伴与学习状态响应；不以角色存在证明坚持度或学习收益。

角色与动作属于第三方资源。上游 dsh-pet 代码为 MIT，README 对动画、提示词和源视频标明禁止商用。不能宣称角色原创或商业授权已完成，详见 [版权与资产核查](docs/ican/third-party-notices.md)。

## 边界与后续证据

- 没有可查证的私有 Flask 完整版，不沿用未经核实的完整版宣传。
- 原课程中的闭源模型规模、性能数字、绝对化机制描述与旧工具配置需独立审校。
- localStorage 模式不等于多租户、云同步、备份恢复或机构生产部署已经完成。
- Firebase 配置为占位符时，数据仅存浏览器；启用 Firebase 后才有跨设备同步，但不等于生产级账号体系。
- `ai-config.js` 中的 DeepSeek 密钥公网可见，任何访问者可读取；不等于安全的密钥管理方案。
- 小题库和启发式讲解检查不能杜绝背题、代答或证明理解。
- 浏览器视口与功能测试不能替代低端真机性能、长期稳定性及教育效果试验。
- 商业化先完成素材授权，再验证付费方、成本与真实持续使用需求。

后续采用 [教育效果研究计划](docs/ican/education-study-plan.md)，先做内容和可用性试点，再研究迁移与延迟保持。比赛陈述与评委追问统一见 [答辩材料入口](docs/ican/README.md)。
